# HANDOFF — Next Conversation Starts Here

> Paste-ready brief for the new chat in the "Kaggle" project.
> Read AGENTS.md + MEMORY.md + CONTEXT.md first; this is the quick orientation.

## Where we are

The ATUM project (atum-app-dna.vercel.app) has: a live trilingual app (7 pages,
deep word pages, 24 concepts with trust badges), complete source extraction
(15 notebooks), and three synthesis docs:
- `marketing/product/SYNTHESIS_phd.md` — 3 external PhD evaluations.
- `data/sources/phd-mining/RESEARCH-ROADMAP.md` — 22 ranked testable ideas.
- New context-engineering system: `AGENTS.md`, `MEMORY.md`, `CONTEXT.md`, `skills/`.

## What's running in parallel (you return with these results)

1. **Open Code → CCM Algorithm** (flagship research). Phases 1–2: encode 9
   phonetic classes from ibdalRules.json, run CCM on 5 language pairs, set up
   the baseline. See `skills/ccm-algorithm/SKILL.md`.

2. **Claude/Cursor → MVP "Root Code Starter Pack"** (product). Package the 20
   strongest word-insights (already exist, 3 languages) into a guide + outline a
   60-min masterclass. Built from existing assets, not from scratch.

## How to work (unchanged, it works)

- User is conductor; Claude writes plans + prompts; agents execute in parallel on
  separate folders; user relays reports. Return to Claude only for review or UI/UX.
- Pick agent by task, not habit. No bias. Anti-Gravity used sparingly (quota).
- Golden rule: never mix 🔬🧩 (academic) with 🔍❓ (metaphysical) in one output.
- The evidence is the statistic, not the count of agreeing sources.
- Don't scatter — finish one mini-project before the next.

## Deferred (don't push prematurely)

Book update · Kaggle publish · domain (Namecheap, last phase) · n8n automation
(only when work repeats enough) · per-source NotebookLM re-query.

## First thing to do next session

Collect the two parallel reports (CCM Phase 1–2 + MVP package), review them,
update MEMORY.md, then decide the next mini-project from the ROADMAP Tier 1.
